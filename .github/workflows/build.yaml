name: Build add-on images

# Builds per-architecture images and publishes them to GHCR as
# ghcr.io/<owner>/ws-scrcpy-ha-<arch>:<version> (+ :latest), matching the
# `image: ghcr.io/.../ws-scrcpy-ha-{arch}` reference in ws-scrcpy/config.yaml.

on:
  push:
    branches:
      - master
      - main
    paths:
      - "src/**"
      - "vendor/**"
      - "webpack/**"
      - "typings/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - "Dockerfile"
      - "run.sh"
      - "ws-scrcpy/config.yaml"
      - ".github/workflows/build.yaml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

# Cancel an in-progress build when a newer commit is pushed to the same ref.
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # amd64 builds natively on the x86 runner.
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-latest
          # aarch64 builds natively on GitHub's free arm64 runner (no QEMU).
          - arch: aarch64
            platform: linux/arm64
            runner: ubuntu-24.04-arm
          # armv7 also runs on the arm64 runner; emulating 32-bit ARM on 64-bit
          # ARM is far cheaper than on x86.
          - arch: armv7
            platform: linux/arm/v7
            runner: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read add-on version
        id: meta
        run: |
          version="$(grep -E '^version:' ws-scrcpy/config.yaml | head -1 | sed -E 's/^version:[[:space:]]*"?([^"]+)"?/\1/')"
          owner="$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "image=ghcr.io/${owner}/ws-scrcpy-ha-${{ matrix.arch }}" >> "$GITHUB_OUTPUT"

      # Only needed for the 32-bit ARM build; amd64 and aarch64 build natively.
      - name: Set up QEMU
        if: matrix.platform == 'linux/arm/v7'
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          provenance: false
          tags: |
            ${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.version }}
            ${{ steps.meta.outputs.image }}:latest
          cache-from: type=gha,scope=${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.arch }}
